[core]
	excludesfile = ~/.gitignore
	editor = vim
	pager = less -r
[user]
	name = Bartosz Marcinkowski
	email = bartosz.marcinkowski@rtbhouse.com
[push]
	default = simple
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
